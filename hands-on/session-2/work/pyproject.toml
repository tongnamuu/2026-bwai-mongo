[project]
name = "rag-session-2"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pymongo>=4.6",
    "sentence-transformers>=3.0",
    "langchain-text-splitters",
    "openai",
    "python-dotenv",
    "tqdm",
    "jupyter",
]
